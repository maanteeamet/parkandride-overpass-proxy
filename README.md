proxy for querying parkandride nodes

```
/*
This has been generated by the overpass-turbo wizard.
*/
[out:json][timeout:25];
// fetch area “estonia” to search in
area(3600079510)->.searchArea;
// gather results
(

  // query part for: “park_ride=*”
  node["amenity"="parking"]["park_ride"="yes"](area.searchArea);
  way["amenity"="parking"]["park_ride"="yes"](area.searchArea);
  relation["amenity"="parking"]["park_ride"="yes"](area.searchArea);

  // query part for: “"bicycle parking"”
  node["amenity"="bicycle_parking"](area.searchArea);
  way["amenity"="bicycle_parking"](area.searchArea);
  relation["amenity"="bicycle_parking"](area.searchArea);
);
// print results
out body;
>;
out skel qt;
```